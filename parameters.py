# This class will set parameters for encryption using the drop down menu
# define parameters as attributes so that object can be passed as an argument
# to the encryption function

# Some parameters can be
#   1. No of iterations
#   2. Values of a, b (arnold cat map)
#   3. Values of mu (logistics)
